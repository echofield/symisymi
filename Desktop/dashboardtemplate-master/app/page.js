import MarketingDashboard from "@/components/Dashboard";

export default function HomePage() {
  // This page should ONLY return the component for the main content area.
  // The Sidebar is already handled by the layout.js file.
  return <MarketingDashboard />;
}