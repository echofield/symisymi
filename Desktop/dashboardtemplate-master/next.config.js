/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
  // Optional: Add domains if you load external images
  images: {
    domains: [],
  },
}

module.exports = nextConfig;